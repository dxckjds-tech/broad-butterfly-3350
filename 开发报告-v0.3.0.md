# AI Store Doctor Page Builder v0.3.0 开发报告（Step 1–4）

- **项目：** AI Store Doctor Page Builder
- **基线版本：** v0.2.1
- **当前版本：** v0.3.0 Step 4（含 Step 1–3）
- **日期：** 2026-09-10
- **原则：** 增量开发。不重构 Editor Core，不改 Canvas / Drag Drop / History，不删除既有组件。

本报告对应本次打包交付。分步原文见：

- `开发报告-v0.3.0-Step1.md`
- `开发报告-v0.3.0-Step2.md`
- `开发报告-v0.3.0-Step3.md`
- `开发报告-v0.3.0-Step4.md`

---

## 1. 目标与完成情况

| Step | 目标 | 状态 |
|---|---|---|
| 1 | MIC 商品详情数据模型 + AI Patch Protocol（AI 不能直接改页） | 完成 |
| 2 | MIC 组件库 + 行业模板 + Schema → 组件树 → Builder Node | 完成 |
| 3 | Suggestion Preview + 用户审批 + Template Center → Canvas | 完成 |
| 4 | AI Agent Layer：多 Agent → AISuggestion（不直接生成页面） | 完成 |

数据通路：

```
MIC Template
    → createMICPageFromTemplate()
    → MICPageSchema
    → convertMICPageToBuilder()
    → Canvas（MIC Component Tree）

AISuggestion（PENDING）
    → 用户接受
    → approveSuggestion + applySuggestion
    → MICPageSchema
    → convertMICPageToBuilder()
    → Canvas
```

---

## 2. 新增文件清单

### 2.1 Step 1 — MIC Schema（`src/mic/schema/`）

| 文件 | 职责 |
|---|---|
| `product.ts` | `ProductSchema`；缺字段 → `''` / `[]` |
| `image.ts` | `ImageSchema`（角色 + PAGE/USER/AI_REFERENCE） |
| `company.ts` | `CompanySchema` |
| `page.ts` | `MICPageSchema`，`version = 0.3.0` |
| `coerce.ts` | 无 `any` 空值收窄 |
| `index.ts` | 导出 |

### 2.2 Step 1 — AI Patch Protocol（`src/ai/protocol/`）

| 文件 | 职责 |
|---|---|
| `patch.ts` | `Patch`、`createPatch`、`applyPatch`（确认门闩） |
| `suggestion.ts` | `AISuggestion`，默认 PENDING |
| `validation.ts` | PASS / WARNING / BLOCKED |
| `index.ts` | 导出 |

### 2.3 Step 1 — 启发式 Adapter（`src/core/adapters/`）

| 文件 | 职责 |
|---|---|
| `micAdapter.ts` | `convertBuilderToMIC` / `convertMICToBuilder`（heading/text/image，有损） |
| `index.ts` | 导出 |

### 2.4 Step 2 — MIC 组件（`src/mic/components/`）

| 文件 | 职责 |
|---|---|
| `MICProductHero.tsx` | 首屏；无图占位；无关键词隐藏标签 |
| `MICFeatureSection.tsx` | 1–6 张优势卡片 |
| `MICSpecificationTable.tsx` | 空数据：`No specifications available` |
| `MICCertification.tsx` | 认证只展示、不生成；预留图片槽 |
| `MICFactoryGallery.tsx` | factory / production / certificate |
| `MICPackaging.tsx` | 包装 + 运输 |
| `MICFAQ.tsx` | 折叠 FAQ，默认全关 |
| `MICCompanyProfile.tsx` | 公司简介 |
| `types.ts` / `readProps.ts` / `shared.tsx` / `index.ts` | 类型、读取、占位、注册 |

组件原则：只接收 props；不读 AI；不修改 Schema。

### 2.5 Step 2 — 模板与转换

| 文件 | 职责 |
|---|---|
| `src/mic/templates/machinery.ts` | 机械：Hero→Feature→Spec→Factory→Cert→Packaging→FAQ |
| `src/mic/templates/electronics.ts` | 电子 |
| `src/mic/templates/consumer.ts` | 消费品 |
| `src/mic/adapters/micComponentAdapter.ts` | 纯函数：Schema → 组件树 → Builder（包 section） |

### 2.6 Step 3 — Suggestion UI / Template Center / 服务

见 `开发报告-v0.3.0-Step3.md`。摘要：`src/ai/suggestion-ui/`、`src/mic/templates/ui/`、`src/mic/services/`、`src/mic/studio/useMicStudio.ts`（local state，非全局 store）。

### 2.7 Step 4 — Agent Layer

见 `开发报告-v0.3.0-Step4.md`。`src/ai/agents/`、`src/ai/providers/`、`src/ai/orchestrator/`。

### 2.8 测试与报告

| 文件 | 职责 |
|---|---|
| `tests/mic-schema.test.ts` | Schema / Patch / 确认门闩 |
| `tests/mic-components.test.tsx` | 组件渲染 / 空数据 / 模板 / 转换 / FAQ |
| `tests/suggestion-ui.test.tsx` | 建议展示 / 审批 / 模板中心 / 禁止自动 apply |
| `tests/agents.test.ts` | Agent 接口 / Vision / Keyword / 外部参数 / Verification / Orchestrator |
| `开发报告-v0.3.0-Step1.md` | Step 1 原文 |
| `开发报告-v0.3.0-Step2.md` | Step 2 原文 |
| `开发报告-v0.3.0-Step3.md` | Step 3 原文 |
| `开发报告-v0.3.0-Step4.md` | Step 4 原文 |
| `开发报告-v0.3.0.md` | 本汇总 |
| `MANIFEST-v0.3.0.txt` | 打包清单 |

---

## 3. 修改的既有文件（仅接线，非重构）

| 文件 | 改动 | 是否 Editor Core |
|---|---|---|
| `vitest.config.ts` | `include` 增加 `tests/**/*.test.ts?(x)` | 否 |
| `src/editor/components/index.ts` | 一行 `import '../../mic/components'` | 否（v0.2.1 官方加组件入口） |
| `scripts/smoke.tsx` | 断言改为「v0.2.1 八个仍在 + MIC 八个已注册」 | 否 |
| `src/editor/layout/LeftPanel.tsx` | Templates 页嵌入 MIC Template Center | 否 |
| `src/editor/layout/TemplatesPanel.tsx` | 可选 `micTemplates` 插槽 | 否 |
| `src/editor/layout/RightPanel.tsx` | Inspect / Suggest 页签 | 否 |
| `src/editor/layout/EditorShell.tsx` | `useMicStudio` props 接线（未改 DnD） | 否 |
| `src/index.css` | risk / diff 主题 token | 否 |

**未修改：** `editor/core/registry.ts`、Canvas、Drop Plan、History/Command、既有 8 个业务组件实现。

MIC 类型通过各文件内 `registerComponent()` 注册，与 Heading 相同。节点 `allowedParents = section | container`，避免改 Root 的 `allowedChildren`。

---

## 4. 关键设计

### 4.1 空值

所有 MIC 字段允许空。规格缺失 → `[]` 并显示 `No specifications available`。FAQ 缺失 → `[]`。无主图 → 占位。无关键词 → 不渲染标签。

### 4.2 AI 不得直接改页

- `createPatch` 对 `AI_GENERATED` / `EXTERNAL_REFERENCE` 强制 `needConfirm: true`
- `applyPatch` 未传 `{ confirmed: true }` 则拒绝
- `applySuggestion` 仅 `APPROVED`
- 认证 / 规格 / 包装路径：WARNING 或 BLOCKED

### 4.3 组件不生成认证

`MICCertification` 只展示传入的 `certifications: string[]`，Inspector 只读。

### 4.4 两套 Adapter

| 函数 | 产出 | 用途 |
|---|---|---|
| `convertMICToBuilder`（Step 1） | heading / text / image | 旧通用节点，有损 |
| `convertMICPageToBuilder`（Step 2） | `mic-*` 组件包在 section 里 | 可在 Page Builder 中编辑 MIC 块 |

Step 2 转换才是详情页编辑路径。

### 4.5 面板插入

空画布点击 MIC 组件会因 Root 不接受 `mic-*` 而无法插入（有意不改 Root）。用法：先放 Section，再把 MIC 组件拖入 / 点选 Section 后点击组件。模板转换已自动包 Section。

---

## 5. 测试结果

```
npm run typecheck   PASS（strict，无 any）
npm run test        13 files, 153/153 PASS
npm run smoke       all checks passed（v0.2.1 + MIC 注册 + Agent 顺序）
```

其中：v0.2.1 原 103；Step 1 新增 18；Step 2 新增 9；Step 3 新增 14；Step 4 新增 9。

---

## 6. 对 v0.2.1 的影响

兼容。`SCHEMA_VERSION` 仍为 `1`。MIC `metadata.version` 为 `0.3.0`，两套版本独立。旧文档、拖拽、历史、自动保存行为不变。组件面板 Basic/Media 下多出 MIC 条目。

---

## 7. 风险

1. Step 1 的 heading/text 映射有损；请用 Step 2 的 `mic-*` 树做详情页。
2. Feature 卡片由 `description` 分段推导，不是独立 schema 字段。
3. Suggestion Preview 与 Template Center 已接到面板（Step 3）。
4. Agent 只产出 PENDING Suggestion（Step 4）；未配置模型时返回 warning，不换品牌。
5. 不要把 Patch 打进现有 History Command。

---

## 8. 打包内容

| 包 | 说明 |
|---|---|
| `ai-store-doctor-page-builder-0.3.0-step3-modules.tar.gz` | 仅 Step 3 新增模块 + 测试 + 报告 |
| `ai-store-doctor-page-builder-0.3.0-step3-src.tar.gz` | 可构建完整源码（无 node_modules / dist / .git） |

模块包解压后：

```
ai-store-doctor-page-builder-0.3.0-step3-modules/
├── MANIFEST.txt
├── 开发报告-v0.3.0-Step3.md
├── src/ai/suggestion-ui/
├── src/mic/services/
├── src/mic/studio/
├── src/mic/templates/ui/
└── tests/suggestion-ui.test.tsx
```

完整源码：

```bash
npm ci
npm run typecheck
npm run test
npm run smoke
npm run dev
```

---

## 9. 下一步（Step 5）建议

1. Suggest 面板四格模型绑定（Vision / Keyword / Content / Verify），不写死品牌。
2. HTTP provider 真正的多模态图片输入。
3. BLOCKED 建议在 UI 只读展示。
4. 接受建议继续走现有 `replacePage`。
