# AI Store Doctor Page Builder v0.3.0 Step 2 开发报告

- **基线：** v0.3.0 Step 1（MIC Schema + AI Patch Protocol）
- **本 Step：** MIC Component Library
- **日期：** 2026-09-10

---

## 1. 新增文件

### 组件 `src/mic/components/`

| 文件 | 说明 |
|---|---|
| `MICProductHero.tsx` | 首屏：图 / 标题 / 卖点 / 关键词；无图占位，无关键词隐藏标签 |
| `MICFeatureSection.tsx` | 优势卡片，最多 6 个 |
| `MICSpecificationTable.tsx` | 参数表；空数据文案 `No specifications available` |
| `MICCertification.tsx` | 认证列表 + 图片槽；只展示、不生成 |
| `MICFactoryGallery.tsx` | 工厂图：`factory` / `production` / `certificate` |
| `MICPackaging.tsx` | 包装 + 运输 |
| `MICFAQ.tsx` | 折叠 FAQ，默认全部关闭 |
| `MICCompanyProfile.tsx` | 公司简介 |
| `index.ts` | 统一导出并 `registerComponent()` |
| `types.ts` | `mic-*` 类型 id |
| `readProps.ts` | 无 `any` 的 props 读取 |
| `shared.tsx` | 占位与标题 |

每个组件都是 **props → render**。编辑器侧另有 chrome 外壳 + Inspector，不读 AI、不写 `MICPageSchema`。

### 模板 `src/mic/templates/`

- `machinery.ts` — Hero → Feature → Spec → Factory → Cert → Packaging → FAQ
- `electronics.ts`
- `consumer.ts`
- `index.ts`

### 转换层 `src/mic/adapters/`

- `micComponentAdapter.ts` — 纯函数  
  `convertMICPageToComponentTree` / `convertMICComponentTreeToBuilder` / `convertMICPageToBuilder`
- `index.ts`

### 测试

- `tests/mic-components.test.tsx`

---

## 2. 修改文件

| 文件 | 改动 | 原因 |
|---|---|---|
| `src/editor/components/index.ts` | 增加一行 `import '../../mic/components'` | 与 v0.2.1「加组件只改这个 barrel」一致；`main.tsx` 与 smoke 才能注册 MIC 类型。**未改 `registry.ts`。** |
| `scripts/smoke.tsx` | 「恰好 8 个组件」改为「v0.2.1 八个仍在 + MIC 八个已注册」 | 原断言在注册 MIC 后会误失败；不是 Canvas/History/DnD |

未修改：Canvas 算法、Drop Plan、History/Command、`registry.ts`、既有 8 个业务组件文件。

**为何不改 `registry.ts`：** `registerComponent()` 已是公开 API。MIC 组件在自己的文件里注册，与 Heading 相同。

**嵌套规则：** MIC 节点 `allowedParents = section | container`，Adapter 把每个 MIC 组件包进 `section`。这样不用改 `RootComponent.allowedChildren`。从左侧面板添加时：先放 Section，再把 MIC 组件放进 Section（或点选 Section 后点击组件）。

---

## 3. 测试结果

```
npm run typecheck   PASS
npm run test        11 files, 130/130 PASS（原 121 + 新增 9）
npm run smoke       all checks passed
```

覆盖：八个组件渲染、空数据不崩、模板顺序、Schema → 组件树 → Builder（`parsePageDocument` ok）、FAQ 默认折叠且只展开当前项。

---

## 4. 是否影响 v0.2.1

| 项 | 影响 |
|---|---|
| 既有 heading/text/button/… | 无删除、无行为改动 |
| Canvas / DnD / History | 未改 |
| `SCHEMA_VERSION` | 仍为 `1` |
| 空画布 / 旧模板 / 持久化 | 行为不变；新类型只在 MIC 文档或从面板插入时出现 |
| 组件面板 | Basic/Media 下多出 MIC 条目，属增量 |
| smoke | 断言改为兼容增量注册，v0.2.1 八个组件仍被检查 |

结论：**v0.2.1 编辑能力兼容**；MIC 是并列组件族。

---

## 5. Step 3 建议

1. **模板入口：** 在 Templates 面板增加 Machinery / Electronics / Consumer（调用 `convertMICPageToBuilder` + `replacePage`）。那会改 `TemplatesPanel.tsx`，需单独批准。
2. **确认后落地：** 把 Step 1 的 `applySuggestion` 结果再跑 `convertMICPageToBuilder`，不要把 Patch 打进现有 History Command。
3. **Hero 点击插入：** 若要空画布一键插入 MIC，再讨论是否扩展 `root.allowedChildren`（改 Root 组件，不是 Canvas 算法）。
4. **认证仍只展示：** Step 3 的 AI 改认证必须走 Patch + WARNING/BLOCKED，不要在组件里生成证书名。
5. **Feature 数据：** 当前由 `description` 分段推导。若要独立卖点字段，再给 `ProductSchema` 加可选 `features`（Step 1 schema 增量，不是重构 core）。
