# AI Store Doctor Page Builder v0.3.0 Step 3 开发报告

- **基线：** v0.3.0 Step 2（MIC 组件库 + 模板 + Adapter）
- **本 Step：** AI Suggestion Preview + Template Center + 用户审批后写入页面
- **日期：** 2026-09-10

---

## 1. 新增文件

### Suggestion UI `src/ai/suggestion-ui/`

| 文件 | 说明 |
|---|---|
| `SuggestionPanel.tsx` | 建议列表。只吃 `AISuggestion[]` props，不调用 AI |
| `SuggestionCard.tsx` | 单条：标题 / 字段 / Before-After / 原因 / 风险 / 接受·拒绝。支持 UPDATE / INSERT / DELETE / MOVE |
| `DiffViewer.tsx` | string / array / object / MOVE 前后对比。颜色走主题 token |
| `RiskBadge.tsx` | LOW 普通优化 / MEDIUM 需要注意 / HIGH 必须确认 |
| `ApprovalActions.tsx` | 接受 / 拒绝。仅 PENDING 可点 |
| `index.ts` | 导出 |

### Template Center `src/mic/templates/ui/`

| 文件 | 说明 |
|---|---|
| `TemplateCenter.tsx` | Machinery / Electronic / Consumer |
| `TemplateCard.tsx` | 名称、适用行业、页面结构预览 |
| `index.ts` | 导出 |

### 服务层 `src/mic/services/`（纯函数，不写 store）

| 文件 | 说明 |
|---|---|
| `templateService.ts` | `listMicTemplates` / `createMICPageFromTemplate` / `createBuilderPageFromTemplate` |
| `suggestionService.ts` | 预览建议（始终 PENDING）、`confirmSuggestion`、`declineSuggestion`、`applyWithoutApproval`（测试守卫） |
| `index.ts` | 导出 |

### 局部状态 `src/mic/studio/useMicStudio.ts`

不是全局 Zustand store。用 React local state 保存当前 `MICPageSchema` 与 `AISuggestion[]`。写入画布只调用已有的 `replacePage(convertMICPageToBuilder(page))`。

### 测试与报告

- `tests/suggestion-ui.test.tsx`
- `开发报告-v0.3.0-Step3.md`

---

## 2. 修改文件

| 文件 | 改动 | 原因 |
|---|---|---|
| `src/editor/layout/LeftPanel.tsx` | 可选 `onSelectMicTemplate`；Templates 页嵌入 `TemplateCenter` | 接线，不改 Canvas |
| `src/editor/layout/TemplatesPanel.tsx` | 可选 `micTemplates` 插槽，原 v0.2.1 预设仍在下方 | 增量 |
| `src/editor/layout/RightPanel.tsx` | Inspect / Suggest 页签；`SuggestionPanel` 走 props | 未改 Inspector 内部 |
| `src/editor/layout/EditorShell.tsx` | `useMicStudio()`，把回调传给左右面板 | **未改** DndContext / sensors / drop 处理 |
| `src/index.css` | `@theme` 增加 `risk-*` / `diff-*` token | 风险色不写死在组件里 |

未修改：Canvas 算法、Drop Plan、History/Command、`registry.ts`、既有业务组件。

**为何不新增全局 store：** Step 3 状态是「当前 MIC 页 + 建议列表」，面板用 props 即可。`useMicStudio` 是 EditorShell 的 local hook，服务层保持纯函数。

---

## 3. 审批流程（禁止自动 apply）

```
AI / 预览生成  →  PENDING
用户点击接受   →  approveSuggestion
               →  applySuggestion（confirmed）
               →  MICPageSchema 更新
               →  convertMICPageToBuilder
               →  现有 replacePage → Canvas
拒绝           →  REJECTED，页面不变
```

`createPreviewSuggestions` 是夹具数据，不是 AI API。`applySuggestion` 对 PENDING 直接失败。

---

## 4. 模板创建流程

```
用户选择 Machinery
  → createMICPageFromTemplate()
  → MICPageSchema
  → convertMICPageToBuilder()
  → Canvas 显示 MIC 组件树
  → 生成 PENDING 预览建议（不写入商品字段）
```

---

## 5. 测试结果

```
npm run typecheck   PASS
npm run test        12 files, 144/144 PASS（原 130 + 新增 14）
npm run smoke       all checks passed
```

覆盖：建议展示、Before/After、Approve、Reject、模板选择、模板生成页面、PENDING 禁止自动 apply。

---

## 6. 是否影响 v0.2.1

| 项 | 影响 |
|---|---|
| Canvas / DnD / History | 无 |
| 既有 Templates 预设 | 仍在 MIC 中心下方 |
| Inspector Content/Style/Advanced | 仍在 Inspect 页签内 |
| SCHEMA_VERSION | 仍为 1 |

---

## 7. 打包内容

| 包 | 说明 |
|---|---|
| `ai-store-doctor-page-builder-0.3.0-step3-modules.tar.gz` | 仅 Step 3 新增文件 + Step 3 报告 |
| `ai-store-doctor-page-builder-0.3.0-step3-src.tar.gz` | 含 Step 3 的完整可构建源码 |

Step 3 增量包：

```
ai-store-doctor-page-builder-0.3.0-step3-modules/
├── MANIFEST.txt
├── 开发报告-v0.3.0-Step3.md
├── src/ai/suggestion-ui/
├── src/mic/services/
├── src/mic/studio/
├── src/mic/templates/ui/
└── tests/suggestion-ui.test.tsx
```

接线改动不在增量包内，见完整源码包中的 `LeftPanel` / `TemplatesPanel` / `RightPanel` / `EditorShell` / `index.css`。

---

## 8. Step 4 建议

1. 把真实 AI 输出接到 `createSuggestion`（仍强制 PENDING），不要让模型调用 `applyPatch`。
2. Suggestion 与 MIC 页持久化（草稿），刷新后仍可审批。
3. HIGH 风险（认证 / 规格）在卡片上强制二次确认。
4. 模板结构预览改成缩略画布，而不是纯文字列表。
5. 接受建议时走现有 History 的 `replacePage` 即可；不要为 Patch 新增 Command 类型。
