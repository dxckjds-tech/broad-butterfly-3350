# 找不到插件目录时看这里

Chrome「加载已解压的扩展程序」必须选 **含有 `manifest.json` 的文件夹**。

不要选仓库根目录，也不要选 `apps/extension` 源码目录。

## 直接选这个（推荐）

仓库根目录下：

**`AI-店铺医生-插件`**

完整路径（本仓库）：

`/workspace/AI-店铺医生-插件`

你本机克隆后一般是：

`你的项目路径/AI-店铺医生-插件`

用户主目录 / 桌面也会同步一份：

- `~/AI-店铺医生-插件`
- `~/Desktop/AI-店铺医生-插件`
- `~/桌面/AI-店铺医生-插件`

Chrome 文件窗口请先点左侧 **Desktop / 桌面**，再选 `AI-店铺医生-插件`。

## 如果这个文件夹不存在

说明还没构建。在项目根目录执行：

```bash
pnpm install
pnpm --filter @trade-ai/extension build
```

构建成功后会自动生成 `AI-店铺医生-插件`。

## 加载步骤

1. Chrome 打开 `chrome://extensions`
2. 打开右上角「开发者模式」
3. 点「加载已解压的扩展程序」
4. 选中 `AI-店铺医生-插件` 文件夹（能看到里面的 `manifest.json`）
5. 打开 Made-in-China 产品页，或 `http://localhost:5173/demo/mic-product.html`
