# 找不到插件目录时看这里

你本机是 **Mac**。Chrome 弹出的窗口如果标题是「桌面 — iCloud」，那是 iCloud 桌面，**云端电脑里的文件不会出现在这里**。

## Mac 上正确做法

1. 下载仓库里的 `AI-店铺医生-插件.zip`
2. 双击解压，得到文件夹 `AI-店铺医生-插件`
3. 把这个文件夹拖到 **本机桌面**（访达左侧点「tao」，不要只停在「桌面 — iCloud」）
4. Chrome → `chrome://extensions` → 开发者模式 → 加载已解压的扩展程序
5. 若已经加载过，点扩展卡片上的 **重新加载**（圆形箭头）
6. 选中解压后的 `AI-店铺医生-插件`（里面有 `manifest.json`）

也可以在访达按 `Cmd + Shift + G`，粘贴：

`/Users/tao/Desktop/AI-店铺医生-插件`

## 后端离线

插件识别 MIC 页面正常，但会请求 **你 Mac 上的** `http://localhost:3000/api`。

云端 API 不会自动出现在你电脑上。本机没开后端时：

- 新版本会用 **本地规则** 出分（不写入数据库）
- 要入库：在项目根目录运行 `pnpm dev:api`（并先启动 Postgres）

## 仓库内位置

- 文件夹：`AI-店铺医生-插件`
- 压缩包：`AI-店铺医生-插件.zip`
