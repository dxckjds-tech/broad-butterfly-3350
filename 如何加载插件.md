# 找不到插件目录时看这里

你本机是 **Mac**。Chrome 弹出的窗口如果标题是「桌面 — iCloud」，那是 iCloud 桌面，**云端电脑里的文件不会出现在这里**。

## Mac 上正确做法

1. 下载仓库里的 `AI-店铺医生-插件.zip`
2. 双击解压，得到文件夹 `AI-店铺医生-插件`
3. 把这个文件夹拖到 **本机桌面**（访达左侧点「tao」，不要只停在「桌面 — iCloud」）
4. Chrome → `chrome://extensions` → 开发者模式 → 加载已解压的扩展程序
5. 在文件窗口左侧点 **tao** 或本机 **桌面**，选中解压后的 `AI-店铺医生-插件`
6. 打开该文件夹能看到 `manifest.json` 后再点「打开」

也可以在访达按 `Cmd + Shift + G`，粘贴你解压后的路径，例如：

`/Users/tao/Desktop/AI-店铺医生-插件`

（若桌面走了 iCloud，路径可能是）

`/Users/tao/Library/Mobile Documents/com~apple~CloudDocs/Desktop`

这时请把文件夹拖到「访达 → tao → 桌面」，或拖到「下载」。

## 仓库内位置

- 文件夹：`AI-店铺医生-插件`
- 压缩包：`AI-店铺医生-插件.zip`
