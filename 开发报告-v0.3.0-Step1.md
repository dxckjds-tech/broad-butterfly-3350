# AI Store Doctor Page Builder v0.3.0 Step 1 开发报告

- **项目名称：** AI Store Doctor Page Builder
- **基线版本：** v0.2.1
- **目标版本：** v0.3.0 Step 1
- **报告日期：** 2026-09-10
- **工作性质：** 生产级项目增量升级（只新增模块，不重构现有核心）

---

## 1. 任务目标

将通用 Page Builder 升级为支持 AI Store Doctor 的 **MIC（Made-in-China）商品详情页** 数据基础，并建立 **AI 不得直接改页** 的修改协议。

本 Step 交付三块能力：

1. 用 TypeScript 描述完整 MIC 产品页面数据结构（字段可全空）。
2. 接收 AI 修改建议时，只能生成 `Patch` / `AISuggestion`，不能写 Builder 树或 Zustand store。
3. 用户确认后才能把 Patch 应用到 `MICPageSchema`（数据层）。UI 未接线。

明确不做：

- 不重构现有架构
- 不修改 Canvas / Layers / History / Drag Drop
- 不删除已有代码
- 不改编辑器 UI
- 不把 Patch 打进现有 `dispatch` / 历史栈

---

## 2. 约束落实情况

| 约束 | 落实 |
|---|---|
| 不重构架构 | MIC / AI / Adapter 为并行目录，不改 `src/editor/core` 分层 |
| 不改 Canvas | `src/editor/canvas/*` 未动 |
| 不改 Drag Drop | `dropPlan.ts` / `EditorShell` 拖拽 / 图层拖放未动 |
| 不改 History | `editorStore` 历史栈、merge/transaction、`commands.ts` 未动 |
| 不删已有代码 | 手写 Inspector、`registerComponent`、`defaultProps` 全部保留 |
| 只允许新增模块 | 业务代码全部新增；唯一既有文件改动见第 4 节 |
| v0.2.1 功能兼容 | `SCHEMA_VERSION` 仍为 `1`；既有 103 个测试 + smoke 全部通过 |

**架构冲突处理（已按「先方案、不直接改」执行）：**

- 没有把 MIC 挂到 `ComponentDefinition`，避免改注册表。
- 没有改 `parsePageDocument` / `validate.ts`，避免收紧已存草稿。
- 没有改 `UPDATE_PROPS` reducer。
- 没有改 `main.tsx`。Adapter 与协议均为纯函数，调用方显式使用。

---

## 3. 新增文件列表

### 3.1 MIC Schema（`src/mic/schema/`）

| 文件 | 行数 | 职责 |
|---|---:|---|
| `product.ts` | 119 | `ProductSchema`、规格 / FAQ / SourceInfo、空值解析 |
| `image.ts` | 75 | `ImageSchema`（角色 + 来源），供后续识图 / 排序 |
| `company.ts` | 49 | `CompanySchema`（`capacity` / `employees` 可选） |
| `page.ts` | 88 | `MICPageSchema` + `MIC_PAGE_VERSION = "0.3.0"` |
| `coerce.ts` | 50 | 无 `any` 的空值收窄 |
| `index.ts` | 29 | 对外导出 |

### 3.2 AI Patch Protocol（`src/ai/protocol/`）

| 文件 | 行数 | 职责 |
|---|---:|---|
| `patch.ts` | 284 | `Patch`、`createPatch`、`applyPatch`（确认门闩） |
| `suggestion.ts` | 102 | `AISuggestion`、批准 / 拒绝、批量应用 |
| `validation.ts` | 129 | `PASS` / `WARNING` / `BLOCKED` |
| `index.ts` | 36 | 对外导出 |

### 3.3 Adapter（`src/core/adapters/`）

| 文件 | 行数 | 职责 |
|---|---:|---|
| `micAdapter.ts` | 222 | `convertBuilderToMIC` / `convertMICToBuilder` |
| `index.ts` | 4 | 对外导出 |

### 3.4 测试

| 文件 | 行数 | 职责 |
|---|---:|---|
| `tests/mic-schema.test.ts` | 401 | Step 1 规定的 5 类用例 + adapter / 确认流 |

---

## 4. 修改文件列表

| 文件 | 改动 | 原因 |
|---|---|---|
| `vitest.config.ts` | `include` 增加 `tests/**/*.test.ts?(x)` | 任务要求测试路径为 `tests/mic-schema.test.ts`，原配置只扫 `src/**/__tests__` |

未修改的稳定模块（抽样确认）：

- `src/editor/canvas/**`
- `src/editor/layers/**`
- `src/editor/store/editorStore.ts`
- `src/editor/core/commands.ts` / `reducer.ts` / `registry.ts` / `schema.ts`
- `src/editor/layout/EditorShell.tsx`
- `src/main.tsx`

说明：本仓库 `main` 原先只有模板 README。为使 Page Builder 可构建、可测试，另有一次提交导入了 **v0.2.1 完整源码**（不是对本轮编辑器逻辑的修改）。

---

## 5. 数据模型

### 5.1 `ProductSchema`

必填字段在类型上始终存在，运行时允许全空：

- `id` / `productName` / `category` / `description` / `packaging` / `companyProfile` → 缺省 `''`
- `keywords` / `images` / `specifications` / `certifications` / `faq` → 缺省 `[]`
- `sourceInfo` → `{ url, platform, externalId }` 全空字符串

解析入口：`parseProductSchema(unknown)`，永不抛错。

例如：没有参数 → `specifications = []`；没有 FAQ → `faq = []`；没有图 → `images = []`。

### 5.2 `ImageSchema`

```
type: main | feature | application | detail | specification | certificate | factory | unknown
source: PAGE | USER | AI_REFERENCE
```

用途：后续 Product Identity Agent、图片识别、画廊排序。当前 Adapter 从 Builder 映射的图片一律 `type: "unknown"`、`source: "PAGE"`。

### 5.3 `CompanySchema`

`name` / `description` / `factoryImages` / `certifications` 可空；`capacity` / `employees` 可选。

### 5.4 `MICPageSchema`

```
product + company + metadata{ createdAt, updatedAt, version }
```

`metadata.version` 为 MIC 模型版本 `0.3.0`，**独立于** Builder 文档的 `schemaVersion: 1`。两套版本不要混用。

---

## 6. AI Patch Protocol

### 6.1 原则

```
AI 输出 → Patch[] / AISuggestion（status=PENDING）
         → validatePatch / validateSuggestion
         → 用户确认（needConfirm + confirmed，或 suggestion APPROVED）
         → applyPatch / applySuggestion 只改 MICPageSchema
         → 不写 EditorNode 树，不进 History
```

### 6.2 `Patch`

| 字段 | 说明 |
|---|---|
| `action` | `UPDATE` / `INSERT` / `DELETE` / `MOVE` |
| `target` | 点分路径，如 `product.productName` |
| `oldValue` / `newValue` | 可选；`UPDATE` 若带 `oldValue` 且不匹配则拒绝（防脏写） |
| `source` | `AI_GENERATED` / `USER` / `EXTERNAL_REFERENCE` |
| `confidence` | `[0, 1]` |
| `needConfirm` | AI / 外部来源默认 `true` |
| `id` / `createdAt` | 自动生成 |

标题改写示例：

```json
{
  "action": "UPDATE",
  "target": "product.productName",
  "oldValue": "Vacuum Cleaner",
  "newValue": "Cordless Handheld Vacuum Cleaner",
  "source": "AI_GENERATED",
  "confidence": 0.92,
  "needConfirm": true
}
```

`createPatch()` 对 `AI_GENERATED` 与 `EXTERNAL_REFERENCE` **强制** `needConfirm: true`，即使调用方漏传。

`applyPatch(page, patch)` 在 `needConfirm === true` 且未传 `{ confirmed: true }` 时返回失败，页面不变。

### 6.3 `AISuggestion`

- `risk`: `LOW` / `MEDIUM` / `HIGH`
- `status`: `PENDING` / `APPROVED` / `REJECTED`
- `createSuggestion()` 始终创建 `PENDING`，AI JSON 不能自批为 `APPROVED` 后直接落地
- `applySuggestion()` 仅当 `status === "APPROVED"` 才逐条 `applyPatch(..., { confirmed: true })`

### 6.4 校验规则

| 规则 | 结果 |
|---|---|
| `AI_GENERATED` 且 `needConfirm !== true` | `BLOCKED` |
| `source === "EXTERNAL_REFERENCE"` | 至少 `WARNING`；无确认则 `BLOCKED` |
| 高风险字段（认证 / 规格 / 包装）有确认 | `WARNING` |
| 高风险字段无确认 | `BLOCKED` |
| 普通 AI 标题改写且已 `needConfirm` | `PASS`（仍须用户确认才能 apply） |

高风险路径前缀：

- `product.certifications`
- `product.specifications`
- `product.packaging`
- `company.certifications`

---

## 7. MIC Adapter（仅数据转换）

`src/core/adapters/micAdapter.ts` 提供：

- `convertBuilderToMIC(page: PageDocument): MICPageSchema`
- `convertMICToBuilder(mic: MICPageSchema): PageDocument`

**Builder → MIC（启发式）：**

- `productName` ← 首个 heading，否则 `page.name`
- `description` ← 所有 text 节点拼接
- `images` ← image 节点（`unknown` / `PAGE`）
- 规格、FAQ、认证、关键词 → 空数组（Builder 尚无对应节点）

**MIC → Builder：**

- 用现有 `root` / `section` / `heading` / `text` / `image` 字面量组树
- 不调用 `registerComponent` / `createNode`，不依赖注册表是否已填充
- 空字段不生成占位 section
- 规格 / FAQ / 认证序列化成文本，避免静默丢文案

不修改 UI，不 `dispatch`。

---

## 8. 测试结果

命令：

```bash
npm run typecheck   # tsc -b，strict
npm run test        # Vitest
npm run smoke       # v0.2.1 SSR 冒烟（未回归）
```

| 检查 | 结果 |
|---|---|
| typecheck | 通过 |
| vitest | **10 个文件，121/121 通过**（原 103 + 新增 18） |
| smoke | 全部通过（注册表 / reducer / schema / presets / 首屏） |

新增测试覆盖：

1. 完整商品数据 round-trip
2. 缺失字段 → 空字符串 / 空数组
3. 空图片数组（含无 image 节点的 Builder 页）
4. AI Patch 生成、`needConfirm` 强制、未确认拒绝、确认后应用、PENDING suggestion 不可 apply
5. Patch validation（PASS / EXTERNAL_REFERENCE WARNING / 高风险 WARNING / 篡改 BLOCKED）
6. Adapter 双向转换（附加，不破坏既有编辑器测试）

---

## 9. 风险与已知限制

1. **映射有损。** v0.2.1 组件没有规格表、FAQ、认证、关键词节点。MIC → Builder → MIC 不能保证结构化字段还原。
2. **未接线。** 编辑器运行时与 v0.2.1 行为一致。不 import MIC 模块则打包体积也不含协议逻辑（除 tree-shaking 外，入口未引用）。
3. **不要把 Patch 送进现有 History。** 现有 Command 是 Builder 树操作。MIC Patch 是另一条数据通路，混用会破坏撤销语义。
4. **高风险列表是前缀表。** 新增敏感字段必须同步 `HIGH_RISK_TARGETS`。
5. **`parsePatch` 会走 `createPatch`。** 从 JSON 进来的 AI patch 会被再次强制 `needConfirm: true`。内存里手搓的 `Patch` 对象仍靠 `validatePatch` 拦截。

---

## 10. 下一步建议（非本 Step）

1. Suggestion 面板：展示 `AISuggestion`，用户点批准后再 `approveSuggestion` + `applySuggestion`。
2. 若要开机加载协议模块，再讨论是否在 `main.tsx` 增加一行 import（改旧文件，需单独批准）。
3. 若要在画布上无损编辑规格 / FAQ / 认证，再新增专用组件类型（不要改 Canvas 核心算法）。
4. 设计「确认后写入 Builder」的专用 Command，而不是复用 `UPDATE_PROPS` 打点分路径。
5. Product Identity Agent / 图片识别可直接消费 `ImageSchema`。

---

## 11. 交付打包

同目录交付两份压缩包（均不含 `node_modules`、`dist`、`.git`）：

| 包名 | 内容 |
|---|---|
| `ai-store-doctor-page-builder-0.3.0-step1-modules.tar.gz` | 仅本 Step 新增模块 + 测试 + 本报告 |
| `ai-store-doctor-page-builder-0.3.0-step1-src.tar.gz` | 可构建的完整源码（v0.2.1 编辑器 + Step 1 模块） |

解压模块包后的根目录：

```
ai-store-doctor-page-builder-0.3.0-step1-modules/
├── 开发报告-v0.3.0-Step1.md
├── MANIFEST.txt
├── src/mic/schema/*
├── src/ai/protocol/*
├── src/core/adapters/*
└── tests/mic-schema.test.ts
```

完整源码包解压后按 v0.2.1 方式运行：

```bash
npm ci
npm run typecheck
npm run test
npm run smoke
npm run dev
```

---

## 12. 结论

Step 1 已在不触碰 Page Builder 稳定核心的前提下，补齐 MIC 商品详情数据模型与 AI Patch 协议。AI 修改默认需要确认；外部参考与认证/规格类改动会升为 WARNING 或 BLOCKED。数据转换与协议均为纯函数，可独立测试，不影响 v0.2.1 编辑、拖拽、历史与持久化。
