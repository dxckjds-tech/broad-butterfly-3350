const state={tab:0,subtab:0,fields:null,report:null,loading:false,elapsed:0,error:'',meta:null};
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const badge=s=>`<span class="badge badge-${String(s||'unknown').toLowerCase()}">${esc(s||'UNKNOWN')}</span>`;
const arr=v=>Array.isArray(v)?v:[];
const translateButton=text=>text?`<button class="translate-btn" data-action="translate" data-text="${encodeURIComponent(String(text))}">翻译</button>`:'';
function summary(){const e=document.getElementById('summary');if(state.loading)e.innerHTML=`<b>AI 正在分析… ${state.elapsed} 秒</b><div class="loader"></div><div class="muted">K3 推理可能需要 60–125 秒，其他模型通常 10–40 秒</div>`;else if(state.error)e.innerHTML=`<div class="danger">${esc(state.error)}</div><button class="pill-btn" data-action="settings">打开 API 设置</button>`;else if(!state.report)e.innerHTML='<b>读取商品 URL 后，点击“AI 分析商品”</b>';else{const s=state.report.summary||{};e.innerHTML=`<div class="summary-line"><b>${esc(s.identity||'身份待确认')}</b><b class="teal">${esc(s.confidence||0)}%</b></div><div>${badge(s.status)}</div><div class="muted">数据完整度 ${esc(s.dataCompleteness||0)} · 内容就绪度 ${esc(s.contentReadiness||0)}</div>`;}}
function tabs(){const x=['概览','商品真相','关键词','内容优化','证据与调试'];document.getElementById('tabs').innerHTML=x.map((n,i)=>`<button class="tab-btn ${state.tab===i?'active':''}" data-action="tab" data-i="${i}">${n}</button>`).join('');}
function overview(r){const s=r.summary||{};return `<div class="card"><div class="section-label">AI 诊断</div>${arr(s.conflicts).length?arr(s.conflicts).map(x=>`<p class="danger">⚠ ${esc(x)}</p>`).join(''):'<p class="success">未发现明确冲突</p>'}</div><div class="card"><b>下一步建议</b>${arr(s.nextActions).map(x=>`<p>• ${esc(x)}</p>`).join('')}</div>`;}
function truth(r){return `<div class="section-label">视觉与字段身份候选</div>${arr(r.identityCandidates).map(x=>`<div class="card"><div class="summary-line"><b>${esc(x.name)}</b><b>${esc(x.confidence)}%</b></div>${arr(x.support).map(v=>`<p class="success">✓ ${esc(v)}</p>`).join('')}${arr(x.oppose).map(v=>`<p class="danger">✕ ${esc(v)}</p>`).join('')}<button class="identity-confirm" data-action="confirm-identity" data-name="${esc(x.name)}">确认是此产品</button></div>`).join('')}<div class="card manual-identity"><b>以上都不符合</b><input id="manualIdentity" placeholder="输入正确的英文商品身份"><button data-action="confirm-manual-identity">确认并重新分析</button></div><div class="section-label">事实台账</div>${arr(r.facts).map(x=>`<div class="card fact"><div><b>${esc(x.label)}</b>：${esc(x.value)}</div>${badge(x.status)}<small>${esc(x.source)} · ${esc(x.note)}</small></div>`).join('')}`;}
function keywords(r){const k=r.keywords||{};return `<div><div class="section-label">当前关键词</div>${arr(k.current).map(x=>`<span class="chip">${esc(x)}</span>`).join('')||'<div class="card card-na">未读取到</div>'}</div><div><div class="section-label">已拦截</div>${arr(k.blocked).map(x=>`<div class="card card-blocked"><b>${esc(x.keyword)}</b><p>${esc(x.reason)}</p></div>`).join('')||'<div class="card card-ok">无</div>'}</div><div><div class="section-label">AI 候选</div>${arr(k.candidates).map(x=>`<div class="card"><div class="summary-line"><b>${esc(x.keyword)}</b><b>${esc(x.matchScore)}</b></div><p>${esc(x.intent)} · ${esc(x.basis)}</p></div>`).join('')}</div>`;}
function detailView(detail){if(!detail)return'<div class="card card-na">暂无可用详情内容</div>';if(typeof detail==='string')return`<div class="card english-card"><p class="preserve-lines">${esc(detail)}</p>${translateButton(detail)}</div>`;const specs=arr(detail.specifications),highlights=arr(detail.highlights),applications=arr(detail.applications);const overview=[detail.headline,detail.overview].filter(Boolean).join('\n');const highlightText=['Key Highlights',...highlights.map(x=>'• '+x)].join('\n');const specText=['Technical Specifications',...specs.map(x=>`${x.name}: ${x.value}`)].join('\n');const applicationText=['Applications',...applications.map(x=>'• '+x)].join('\n');const copyText=[overview,highlights.length?highlightText:'',specs.length?specText:'',applications.length?applicationText:'',detail.packagingDelivery?`Packaging & Delivery\n${detail.packagingDelivery}`:'',detail.buyerNote?`Buyer Note\n${detail.buyerNote}`:''].filter(Boolean).join('\n\n');return`<div class="detail-layout">${detail.headline?`<div class="detail-hero"><span>PRODUCT OVERVIEW</span><h2>${esc(detail.headline)}</h2><p>${esc(detail.overview)}</p>${translateButton(overview)}</div>`:''}${highlights.length?`<div class="card english-card"><h3>Key Highlights</h3><ul class="highlight-list">${highlights.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>${translateButton(highlightText)}</div>`:''}${specs.length?`<div class="card english-card"><h3>Technical Specifications</h3><div class="spec-table">${specs.map(x=>`<div class="spec-row"><b>${esc(x.name)}</b><span>${esc(x.value)}</span></div>`).join('')}</div>${translateButton(specText)}</div>`:''}${applications.length?`<div class="card english-card"><h3>Applications</h3><div class="application-grid">${applications.map(x=>`<span>${esc(x)}</span>`).join('')}</div>${translateButton(applicationText)}</div>`:''}${detail.packagingDelivery?`<div class="card detail-info english-card"><h3>Packaging & Delivery</h3><p>${esc(detail.packagingDelivery)}</p>${translateButton(detail.packagingDelivery)}</div>`:''}${detail.buyerNote?`<div class="card detail-note english-card"><h3>Buyer Note</h3><p>${esc(detail.buyerNote)}</p>${translateButton(detail.buyerNote)}</div>`:''}<button class="copy detail-copy" data-action="copy" data-text="${esc(copyText)}">复制完整详情</button></div>`;}
function geoView(geo){if(!geo||geo==='NOT_AVAILABLE')return'<div class="card card-na"><b>当前证据不足</b><p>请补充产品或公司信息后重新分析，系统不会编造 GEO 内容。</p></div>';if(typeof geo==='string')return`<div class="card english-card"><p class="preserve-lines">${esc(geo)}</p>${translateButton(geo)}</div>`;const facts=arr(geo.productFacts),questions=arr(geo.buyerQuestions),guidance=arr(geo.sourcingGuidance),evidence=arr(geo.evidenceBasis);const allText=[geo.headline,geo.directAnswer,facts.join('\n'),geo.companyContext,questions.map(x=>`Q: ${x.question}\nA: ${x.answer}`).join('\n\n'),guidance.join('\n'),evidence.join('\n')].filter(Boolean).join('\n\n');return`<div class="geo-layout">${geo.headline?`<div class="detail-hero"><span>AI-READY PRODUCT ANSWER</span><h2>${esc(geo.headline)}</h2><p>${esc(geo.directAnswer)}</p>${translateButton([geo.headline,geo.directAnswer].join('\n'))}</div>`:''}${facts.length?`<div class="card english-card"><h3>Verified Product Facts</h3><ul class="highlight-list">${facts.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>${translateButton(facts.join('\n'))}</div>`:''}${geo.companyContext?`<div class="card english-card geo-company"><h3>Supplier Context</h3><p>${esc(geo.companyContext)}</p>${translateButton(geo.companyContext)}</div>`:''}${questions.map(x=>{const text=`Q: ${x.question}\nA: ${x.answer}`;return`<div class="card english-card"><h3>${esc(x.question)}</h3><p>${esc(x.answer)}</p>${translateButton(text)}</div>`;}).join('')}${guidance.length?`<div class="card english-card detail-note"><h3>Sourcing Guidance</h3><ul class="highlight-list">${guidance.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>${translateButton(guidance.join('\n'))}</div>`:''}${evidence.length?`<details class="card geo-evidence"><summary>Evidence Basis</summary>${evidence.map(x=>`<p>• ${esc(x)}</p>`).join('')}</details>`:''}<button class="copy detail-copy" data-action="copy" data-text="${esc(allText)}">复制完整 GEO 文案</button></div>`;}
function content(r){const c=r.content||{},names=['标题','详情','FAQ','GEO'];let body='';if(state.subtab===0)body=arr(c.titles).map(x=>`<div class="card english-card"><b>${esc(x.style)}</b><p>${esc(x.text)}</p><small>采用：${esc(arr(x.factsUsed).join('、'))}<br>排除：${esc(arr(x.excluded).join('、'))}</small><div class="card-actions"><button class="copy" data-action="copy" data-text="${esc(x.text)}">复制</button>${translateButton(x.text)}</div></div>`).join('');else if(state.subtab===1)body=detailView(c.detail);else if(state.subtab===2)body=arr(c.faq).map(x=>{const text=`Q: ${x.question}\nA: ${x.answer}`;return`<div class="card english-card"><b>Q：${esc(x.question)}</b><p>A：${esc(x.answer)}</p>${translateButton(text)}</div>`;}).join('');else body=geoView(c.geo);return `<div class="subtabs">${names.map((x,i)=>`<button class="subtab-btn ${state.subtab===i?'active':''}" data-action="subtab" data-i="${i}">${x}</button>`).join('')}</div>${body}`;}
function debug(r){const d=r.debug||{};return `<div class="card"><b>真实页面读取</b><p>标题：${state.fields?.title?'已读取':'未读取'}</p><p>类目：${state.fields?.category?'已读取':'未读取'}</p><p>规格/表格：${arr(state.fields?.specs).length} 条</p><p>表单字段：${arr(state.fields?.formFields).length} 条</p><p>页面/框架：${state.fields?.frameCount||1} 个</p><p>正文证据：${state.fields?.visibleText?.length||0} 字符</p><p>图片：${arr(state.fields?.images).length} 张</p></div><div class="card"><b>API 调用</b><p>提供商：${esc(state.meta?.provider||'—')}</p><p>模型：${esc(state.meta?.model||'—')}</p><p>请求次数：${esc(state.meta?.attempts||1)}</p><p>Token：${esc(state.meta?.usage?.total_tokens||'—')}</p><p>读取地址：${esc(state.fields?.url||'—')}</p></div><div class="card"><b>缺失字段</b>${arr(d.missingFields).map(x=>`<p>• ${esc(x)}</p>`).join('')||'<p>无</p>'}</div><div class="card"><b>警告</b>${arr(d.warnings).map(x=>`<p class="danger">• ${esc(x)}</p>`).join('')||'<p>无</p>'}</div>`;}
function parseHtml(html,url){const doc=new DOMParser().parseFromString(html,'text/html');const jsonLd=Array.from(doc.querySelectorAll('script[type="application/ld+json"]')).flatMap(n=>{try{const v=JSON.parse(n.textContent);return Array.isArray(v)?v:[v];}catch{return[];}});const product=jsonLd.find(x=>x?.['@type']==='Product'||arr(x?.['@type']).includes('Product'))||{};const rows=Array.from(doc.querySelectorAll('table tr')).map(row=>Array.from(row.querySelectorAll('th,td')).map(c=>c.textContent.trim().replace(/\s+/g,' ')).filter(Boolean)).filter(c=>c.length>=2&&c.length<=8).map(c=>c.join('：'));const defs=Array.from(doc.querySelectorAll('dt')).map(dt=>dt.nextElementSibling?.tagName==='DD'?`${dt.textContent.trim()}：${dt.nextElementSibling.textContent.trim()}`:'').filter(Boolean);const props=arr(product.additionalProperty).map(p=>`${p.name||p.propertyID||'属性'}：${p.value||''}`);const text=(doc.querySelector('main,[role="main"],#content,.main-content,.product-main')||doc.body)?.textContent.replace(/\s+/g,' ').trim().slice(0,30000)||'';const title=product.name||doc.querySelector('h1')?.textContent.trim()||doc.title;const images=Array.from(doc.images).map(img=>({src:new URL(img.getAttribute('src')||'',url).href,alt:img.alt||''})).filter(x=>x.src).slice(0,20);return{title,category:doc.querySelector('.category-breadcrumb,[class*="breadcrumb" i],[class*="category-path" i]')?.textContent.trim()||null,keywords:[],specs:[...new Set([...rows,...defs,...props])].filter(x=>x.length>=3&&x.length<=500).slice(0,160),certifications:[],description:product.description||doc.querySelector('meta[name="description"]')?.content||null,sku:product.sku||product.mpn||null,brand:typeof product.brand==='string'?product.brand:product.brand?.name||null,visibleText:text,images,pageTitle:doc.title,readAt:new Date().toISOString(),url};}
function render(){summary();tabs();const c=document.getElementById('content');if(!state.report)c.innerHTML=state.fields?'<div class="card">商品 URL 数据已读取。点击下方按钮调用所选 AI 完成诊断。</div>':'<div class="card">请粘贴商品 URL，或读取当前页 URL。</div>';else c.innerHTML=[overview,truth,keywords,content,debug][state.tab](state.report);}
async function readUrl(url){state.error='';state.report=null;state.loading=false;if(!url){state.fields=null;state.error='请先粘贴完整商品 URL';render();return false;}document.getElementById('dockUrl').textContent='正在通过登录会话读取 URL…';render();const r=await chrome.runtime.sendMessage({type:'REQUEST_URL_FIELDS',url});if(r?.ok){state.fields=r.fields;document.getElementById('productUrl').value=r.url;document.getElementById('dockUrl').textContent=r.url;render();return true;}state.fields=null;state.error=r?.reason||'URL 读取失败';document.getElementById('dockUrl').textContent=r?.loginRequired?'需要登录':`URL 读取失败：${r?.reason||'未知错误'}`;render();return false;}
async function read(){const r=await chrome.runtime.sendMessage({type:'GET_ACTIVE_URL'});if(r?.ok){document.getElementById('productUrl').value=r.url;return await readUrl(r.url);}state.error=r?.reason||'无法取得当前页 URL';state.loading=false;render();return false;}
async function analyze(){if(!state.fields){const ok=await readUrl(document.getElementById('productUrl').value.trim());if(!ok||!state.fields){state.loading=false;render();return;}}state.loading=true;state.elapsed=0;state.error='';render();const timer=setInterval(()=>{state.elapsed+=1;summary();},1000);try{const r=await chrome.runtime.sendMessage({type:'ANALYZE_PRODUCT',fields:state.fields});if(r?.ok){state.report=r.result;state.meta={provider:r.provider,model:r.model,usage:r.usage,attempts:r.attempts,visionUsed:r.visionUsed};}else state.error=r?.reason||'AI 分析失败';}catch(error){state.error=`AI 请求中断：${error.message||error}`;}finally{clearInterval(timer);state.loading=false;render();}}
document.getElementById('scenarioToggle').textContent='API 设置';document.getElementById('scenarioToggle').dataset.action='settings';document.querySelector('[data-action="sync-task"]').textContent='AI 分析商品';document.querySelector('[data-action="sync-task"]').dataset.action='analyze';
document.getElementById('panel').addEventListener('click',async e=>{const x=e.target.closest('[data-action]');if(!x)return;if(x.dataset.action==='tab')state.tab=Number(x.dataset.i);else if(x.dataset.action==='subtab')state.subtab=Number(x.dataset.i);else if(x.dataset.action==='settings'){chrome.runtime.sendMessage({type:'OPEN_OPTIONS'});return;}else if(x.dataset.action==='read-url'){await readUrl(document.getElementById('productUrl').value.trim());return;}else if(x.dataset.action==='reload-fields'){await read();return;}else if(x.dataset.action==='analyze'){await analyze();return;}else if(x.dataset.action==='confirm-identity'){state.fields.userConfirmedIdentity=x.dataset.name;await analyze();return;}else if(x.dataset.action==='confirm-manual-identity'){const name=document.getElementById('manualIdentity')?.value.trim();if(name){state.fields.userConfirmedIdentity=name;await analyze();}return;}else if(x.dataset.action==='translate'){const original=x.textContent;x.disabled=true;x.textContent='翻译中…';const result=await chrome.runtime.sendMessage({type:'TRANSLATE_TEXT',text:decodeURIComponent(x.dataset.text)}).catch(error=>({ok:false,reason:error.message}));x.disabled=false;x.textContent=original;let box=x.closest('.english-card,.detail-hero,.card');let translated=box.querySelector('.translation-result');if(!translated){translated=document.createElement('div');translated.className='translation-result';box.appendChild(translated);}translated.textContent=result?.ok&&result.translation?result.translation:`翻译失败：${result?.reason||'未知错误'}`;return;}else if(x.dataset.action==='copy'){await navigator.clipboard.writeText(x.dataset.text);x.textContent='已复制';return;}render();});document.getElementById('productUrl').addEventListener('keydown',e=>{if(e.key==='Enter')readUrl(e.currentTarget.value.trim());});render();read();
